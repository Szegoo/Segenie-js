{
  "name": "segenie-js",
  "version": "0.1.0",
  "description": "The npm package used to retrieve the portals of a user",
  "main": "index.js",
  "scripts": {
    "dev": "npx ts-node ./src/index",
    "prettier": "prettier --config ./.prettierrc --write \"**/*.{json,md,ts,js}\"",
    "lint": "eslint . --ext .ts",
    "dist": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "segenie",
    "ic"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "prettier": "^2.8.1"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.46.1",
    "@typescript-eslint/parser": "^5.46.1",
    "eslint": "^8.30.0"
  }
}
